/**
 * Checks if value is array
 * 
 * @param value - the input value to check
 * @returns true if input value is array, otheriwse false
 */
export function IsArray(value: any): boolean {
  return Array.isArray(value);
}